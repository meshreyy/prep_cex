version: "3.9"
services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    network_mode: host
    env_file:
      - ./backend/.env
    restart: unless-stopped

  engine:
    build:
      context: .
      dockerfile: engine/Dockerfile
    network_mode: host
    env_file:
      - ./engine/.env
    restart: unless-stopped
