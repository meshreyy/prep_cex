# Production (Vercel): your deployed backend URL, no trailing slash
VITE_API_URL=https://your-backend.example.com

# Local dev: leave empty to use Vite proxy (/api -> http://localhost:3000)
# VITE_API_URL=
